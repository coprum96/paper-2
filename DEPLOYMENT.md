# 🚀 Развертывание приложения

## Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Приложение доступно на http://localhost:5173
```

## Производственная сборка

```bash
# Сборка проекта
npm run build

# Результат в папке dist/
```

## Развертывание на Vercel

1. Установите Vercel CLI:
```bash
npm i -g vercel
```

2. Разверните:
```bash
vercel
```

3. Следуйте инструкциям

## Развертывание на Netlify

1. Соберите проект:
```bash
npm run build
```

2. Загрузите папку `dist/` на Netlify

Или используйте Netlify CLI:
```bash
npm i -g netlify-cli
netlify deploy --prod --dir=dist
```

## Развертывание на GitHub Pages

1. Установите gh-pages:
```bash
npm install --save-dev gh-pages
```

2. Добавьте в `package.json`:
```json
{
  "homepage": "https://yourusername.github.io/buratino",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}
```

3. Обновите `vite.config.ts`:
```typescript
export default defineConfig({
  plugins: [react()],
  base: '/buratino/' // название репозитория
})
```

4. Разверните:
```bash
npm run deploy
```

## Настройка изображений

Убедитесь что изображения находятся в `public/img/`:
```
public/
  img/
    character_buratino_happy.png
    character_cricket_wise.png
    character_karabas_aggressive.png
    item_gold_coins_5.png
    ... другие изображения
```

## Переменные окружения

Создайте `.env` файл для локальной разработки:
```env
VITE_APP_TITLE="Буратино и Золотой Детектор"
VITE_APP_VERSION="1.0.0"
```

## Оптимизация

### Минимизация размера сборки:
```bash
npm run build -- --mode production
```

### Анализ сборки:
```bash
npm install --save-dev rollup-plugin-visualizer
```

Добавьте в `vite.config.ts`:
```typescript
import { visualizer } from 'rollup-plugin-visualizer'

export default defineConfig({
  plugins: [
    react(),
    visualizer()
  ]
})
```

## Производительность

- Все изображения оптимизированы
- Lazy loading компонентов
- Code splitting автоматически
- Минификация CSS и JS

## Поддержка браузеров

- Chrome (последние 2 версии)
- Firefox (последние 2 версии)
- Safari (последние 2 версии)
- Edge (последние 2 версии)

## Безопасность

- Нет внешних API запросов
- Все данные статичные
- CSP заголовки рекомендуются

## Мониторинг

Рекомендуется добавить:
- Google Analytics
- Sentry для ошибок
- Hotjar для UX аналитики

## Обновления

```bash
# Обновить зависимости
npm update

# Проверить устаревшие
npm outdated
```


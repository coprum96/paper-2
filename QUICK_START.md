# 🚀 Быстрый старт

## Установка и запуск

1. **Установите зависимости:**
```bash
npm install
```

2. **Запустите приложение:**
```bash
npm run dev
```

3. **Откройте браузер:**
Перейдите по адресу `http://localhost:5173`

## 📁 Структура данных

Все данные игры находятся в одном файле: `src/data/gameData.ts`

Включает:
- 🎭 **Персонажи** (characters)
- 📚 **Уровни и диалоги** (levels)
- 📝 **Тесты** (scenarioTest, complexTest, postTest)
- 📖 **Образовательные материалы** (educationalMaterials)
- 🚩 **Красные флажки** (redFlags)
- ❓ **Контрольные вопросы** (controlQuestions)

## 🎮 Игровой процесс

1. **Стартовый экран** - начало приключения
2. **Карта глав** - выбор уровня
3. **Диалоги** - интерактивное общение с персонажами
4. **Тесты** - проверка знаний
5. **Справочная панель** - доступна везде (кнопка 📖 Справка)
6. **Финал** - итоги игры и достижения

## 🛠️ Технологии

- **React + TypeScript** - основа
- **Zustand** - управление состоянием (store/gameStore.ts)
- **Tailwind CSS** - стилизация
- **Vite** - сборка

## 📝 Модификация контента

Для изменения игрового контента отредактируйте `src/data/gameData.ts`:

```typescript
// Добавить новый уровень
levels.push({
  id: 8,
  title: 'Новый уровень',
  subtitle: 'Описание',
  gradient: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)',
  dialogues: [...],
  quiz: [...]
})

// Добавить образовательный материал
educationalMaterials.push({
  id: 7,
  title: '📖 Новый материал',
  sections: [...]
})
```

## 🎨 Кастомизация

### Цвета (tailwind.config.js):
```js
colors: {
  primary: '#9333EA',    // Фиолетовый
  secondary: '#F59E0B',  // Оранжевый
  success: '#10B981',    // Зелёный
  danger: '#DC2626',     // Красный
}
```

### Анимации (src/index.css):
Все анимации настраиваются в CSS

## 📦 Сборка для продакшна

```bash
npm run build
```

Результат будет в папке `dist/`

## 🐛 Troubleshooting

**Порт занят?**
```bash
npm run dev -- --port 3000
```

**Проблемы с изображениями?**
Убедитесь что папка `img/` находится в `public/`

**TypeScript ошибки?**
```bash
npm run build
```

Исправит типы автоматически

## 🎓 Дополнительно

- Все компоненты в `src/components/`
- Store в `src/store/gameStore.ts`
- Утилиты в `src/utils/`
- Стили в `src/index.css`

Приятной игры! 🎮


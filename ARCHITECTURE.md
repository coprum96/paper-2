# 🏗️ Архитектура приложения

## Структура проекта

```
buratino-premium/
├── public/                      # Статические файлы
│   ├── img/                     # Изображения персонажей и предметов
│   └── vite.svg                 # Иконка
├── src/
│   ├── components/              # React компоненты
│   │   ├── StartScreen.tsx      # Стартовый экран
│   │   ├── ChapterMap.tsx       # Карта уровней
│   │   ├── DialogueScene.tsx    # Диалоговая система
│   │   ├── QuizScreen.tsx       # Квизы
│   │   ├── ScenarioTest.tsx     # Сценарный тест
│   │   ├── PostTest.tsx         # Финальный тест
│   │   ├── EndingScreen.tsx     # Экран завершения
│   │   ├── StatsBar.tsx         # Панель статистики
│   │   └── MaterialsPanel.tsx   # Справочная панель
│   ├── data/
│   │   └── gameData.ts          # ВСЕ данные игры
│   ├── store/
│   │   └── gameStore.ts         # Zustand store
│   ├── utils/
│   │   └── particles.ts         # Система частиц
│   ├── App.tsx                  # Главный компонент
│   ├── main.tsx                 # Точка входа
│   ├── index.css                # Глобальные стили
│   └── vite-env.d.ts            # TypeScript типы
├── index-react.html             # HTML шаблон
├── package.json                 # Зависимости
├── tsconfig.json                # TypeScript конфиг
├── vite.config.ts               # Vite конфиг
├── tailwind.config.js           # Tailwind конфиг
└── postcss.config.js            # PostCSS конфиг
```

## Поток данных

```
gameData.ts → gameStore.ts → Components
     ↓              ↓              ↓
  [Данные]    [Состояние]    [UI Рендер]
```

### 1. Данные (gameData.ts)

Все игровые данные в одном месте:
- **characters**: персонажи с эмоджи и изображениями
- **levels**: уровни с диалогами и квизами
- **scenarioTest**: тест на определение обмана
- **complexTest**: сложный тест
- **postTest**: финальный тест
- **educationalMaterials**: обучающие материалы
- **redFlags**: красные флажки мошенничества
- **controlQuestions**: контрольные вопросы
- **tips**: полезные советы

### 2. Состояние (gameStore.ts)

Zustand store управляет:
- **coins**: количество монет
- **wisdom**: уровень мудрости (0-100%)
- **currentLevel**: текущий уровень
- **currentDialogue**: текущий диалог
- **completedLevels**: пройденные уровни
- **achievements**: достижения
- **phase**: текущая фаза игры
- **showMaterialsPanel**: показать/скрыть справку

Actions:
- `setPhase()`: переключение фаз
- `addCoins()`: добавить монеты
- `addWisdom()`: добавить мудрость
- `nextLevel()`: следующий уровень
- `completeLevel()`: завершить уровень
- `resetGame()`: сброс игры

### 3. Компоненты

#### StartScreen
- Стартовый экран с счетчиком игроков
- Анимированный заголовок
- Кнопка начала игры

#### ChapterMap
- Сетка уровней 3x3
- Индикаторы прогресса (✅ пройдено, ⭐ текущий, 🔒 заблокировано)
- Градиенты для каждого уровня
- Клик для начала уровня

#### DialogueScene
- Отображение персонажа (изображение или эмодзи)
- Typewriter эффект для текста
- Красные флажки (если есть)
- Выбор ответов с иконками
- Обработка выборов (wisdom, coins)

#### QuizScreen
- Квиз после каждого уровня
- Выбор ответа из вариантов
- Визуальная обратная связь (✅/❌)
- Объяснение ответа
- Результаты в конце

#### ScenarioTest
- Специальный тест для уровня 2
- Определение ловушек
- Подсчет правильных ответов
- Ранги: Эксперт/Детектив/Ученик

#### PostTest
- Финальный тест на 10 вопросов
- Проверка всех знаний
- Сохранение результата в store

#### EndingScreen
- Итоговая статистика
- Достижения
- Полезные советы
- Кнопки: играть снова, открыть справку

#### StatsBar
- Фиксированная панель вверху справа
- Монеты (🪙)
- Мудрость с прогресс-баром (💡)
- Анимации при изменении

#### MaterialsPanel
- Модальное окно со справкой
- 3 раздела: Материалы, Вопросы, Флажки
- 6 образовательных материалов
- Навигация между разделами

## Фазы игры

```
landing → chapterMap → dialogue → quiz → chapterMap → ... → postTest → ending
```

1. **landing**: стартовый экран
2. **chapterMap**: выбор уровня
3. **dialogue**: прохождение диалогов
4. **quiz**: тест после уровня
5. **scenarioTest**: специальный тест (уровень 2)
6. **postTest**: финальный тест
7. **ending**: завершающий экран

## Система взаимодействия

### Выбор в диалогах

```typescript
{
  text: "Текст выбора",
  icon: "😊",
  wisdom: 10,     // +10 мудрости
  coins: -5,      // -5 монет
  next: 1,        // к диалогу #1
  nextLevel: true // к следующему уровню
}
```

### Квиз вопрос

```typescript
{
  text: "Вопрос?",
  options: ["A", "B", "C"],
  correct: 1,  // индекс правильного ответа
  explanation: "Объяснение",
  reward: 15  // +15 мудрости
}
```

## Анимации

### CSS Animations
- `float`: плавание (персонажи, кнопки)
- `gradientShift`: анимация градиента фона
- `particleFall`: падение частиц
- `slide-in-top`: въезд сверху
- `slide-in-bottom`: въезд снизу
- `scale-in`: увеличение

### Tailwind Animations
Настроены в `tailwind.config.js`:
- Длительности и кривые Безье
- Задержки через `animation-delay`

## Стили

### Tailwind утилиты
- `glass-card`: стеклянный эффект
- `btn`: базовая кнопка
- `btn-primary`: главная кнопка
- `gradient-bg`: анимированный фон

### Кастомные классы
- `.particle`: частицы для анимаций
- `.text-shadow`: тень текста
- `.text-shadow-glow`: свечение текста

## TypeScript типы

Все типы определены в `gameData.ts`:
- `Character`: персонаж
- `Choice`: выбор в диалоге
- `Dialogue`: диалог
- `QuizQuestion`: вопрос квиза
- `Level`: уровень
- `ScenarioTestItem`: пункт сценарного теста
- `EducationalSection`: секция материала
- `EducationalMaterial`: образовательный материал

## Производительность

### Оптимизации
- Lazy loading компонентов (можно добавить)
- Мемоизация с `useMemo`/`useCallback` (где нужно)
- Виртуализация длинных списков (не требуется)
- Code splitting автоматически Vite

### Bundle size
- React: ~40KB gzipped
- Zustand: ~1KB gzipped
- Tailwind: ~10KB (после purge)
- Игровые данные: ~50KB

Общий размер: **~100KB gzipped**

## Расширяемость

### Добавить новый уровень

```typescript
// в gameData.ts
levels.push({
  id: 8,
  title: 'Новый уровень',
  subtitle: 'Описание',
  gradient: 'linear-gradient(...)',
  dialogues: [
    {
      character: 'cricket',
      text: 'Текст диалога',
      choices: [...]
    }
  ],
  quiz: [...]
})
```

### Добавить нового персонажа

```typescript
// в gameData.ts
characters.newCharacter = {
  name: "Имя",
  emoji: "🎭",
  image: "path.png",
  emotions: {...}
}
```

### Добавить новую фазу

1. Добавить тип в `gameStore.ts`
2. Создать компонент
3. Добавить в `App.tsx`

## Тестирование

### Рекомендуемые тесты
- Unit тесты для store
- Component тесты
- E2E тесты с Playwright
- Визуальные регрессии

### Пример теста

```typescript
import { renderHook } from '@testing-library/react'
import { useGameStore } from './gameStore'

test('adds wisdom', () => {
  const { result } = renderHook(() => useGameStore())
  result.current.addWisdom(10)
  expect(result.current.wisdom).toBe(10)
})
```

## Безопасность

- Нет XSS уязвимостей (React escaping)
- Нет SQL injection (нет БД)
- Нет внешних API
- Все данные статичные

## Доступность (A11y)

Рекомендуется добавить:
- ARIA labels
- Keyboard navigation
- Screen reader support
- Focus management
- Color contrast (уже соблюдён)

## Интернационализация

Для добавления переводов:
1. Установить `react-i18next`
2. Вынести строки в JSON
3. Добавить переключатель языка

## Мониторинг

Рекомендуется:
- Error boundary для ошибок
- Analytics для метрик
- Performance monitoring
- User feedback система

